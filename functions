#include <stdio.h>

FUNÇÃO INVERTER 

char *inverter(char *s) {
    int i;
    int tam = 0;

    while (s[tam] != '\0') {
        tam++;
    }

    for(i = 0; i < tam / 2; i++) {
        char tmp = s[i]; 
       s[i] = s[tam - i - 1]; 
       s[tam - i - 1] = tmp; 
    }
    return s;
}
